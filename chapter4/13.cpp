/*
Task 13: Write the program, the argument of function is char array with text.
Result of function returns length of most longest word in text, 
containing in text.
*/
#include <iostream>

unsigned get_longest_word_size(const char * const text) {
   
  return 0;
}

int main() {
  const char text[] = "Result of function returns length of most longest word in text, containing in text";
  std::cout << get_longest_word_size(text) << std::endl;
  return EXIT_SUCCESS;
}